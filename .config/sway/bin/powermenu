#! /bin/bash

chosen=$(printf "  Power Off\n  Restart\n  Lock" | rofi -dmenu -i -theme-str '@import "power.rasi"')

case "$chosen" in
	"  Power Off") loginctl poweroff ;;
	"  Restart") loginctl reboot ;;
	"  Lock") swaylock -f -i ~/.backgrounds/background04.jpg ;; 
	*) exit 1 ;;
esac
